// Vetor de numeros 


var add = 0, ord = 0, aux = 0;



for (let add = 0; add < 7; add++) {
    vetorNumeros[add] = parseInt(prompt("Informe o " + (add + 1)))
}

for (let add = 0; add < 7; add++) {
    for (let ord = add + 1; ord < 7; ord++) {
        if (vetorNumeros[add] > vetorNumeros[ord]){
            
        }
    }
}